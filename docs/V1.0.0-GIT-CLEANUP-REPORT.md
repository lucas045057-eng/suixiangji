# V1.0.0 Git 清理与语义审计报告

审计日期：2026-09-13
审计基线：`origin/main` / `02f2bd0`
目标发布：`v1.0.0`

## 结论

`feat/v1-user-system` 的三次独有提交没有形成当前主线缺失的用户系统能力。当前主线通过 Auth 模块重构、LocalStateSession、用户分区、删除恢复和 Sync session protection 等提交完成了等价实现，并保留了对应回归测试。因此不需要把旧分支整体合并；在最终 bundle、远程 main/tag 和本报告确认完成后，允许删除该旧开发分支。

本报告只授权清理开发分支，不删除历史 Tag `modular-monolith-v1` 和 `sync-v1.0-rc1`。

## 旧分支范围

`feat/v1-user-system` 当前解析到 `374a2c2`，相对于当时分叉点包含三次独有提交：

| Commit | 内容 | 当前 main 的语义承载 | 决定 |
| --- | --- | --- | --- |
| `67e67db` | Backend Beta 多用户、邀请码、JWT auth version、租户约束、`0002_beta_users` | `66e7d9e`、`3afbca0`、`4d92b62`、当前 `app/auth/*`、`app/core/security.py`、`migrations/versions/0002_beta_users.py` | 不移植；允许删除 |
| `2b3ec26` | Flutter 登录/注册、用户分区、注销清理、凭据并发与离线恢复 | `a45b200`、`261f0e1`、`30a9752`、`45c6707`、`976d907`、`d90cd01`、`e206ba3`、当前 `AuthStore`/`FinanceStore`/`LocalRepository` | 不移植；允许删除 |
| `374a2c2` | Beta 用户系统的计划、验证与安全审查记录 | 当前测试矩阵、模块边界文档和本报告 | 不移植；允许删除 |

## 逐项语义审计

| 审计项 | 当前 main 证据 | 结果 |
| --- | --- | --- |
| 注册 | `app/auth/router.py`、`app/auth/registration.py`；邀请码条件更新、用户与默认分类同事务创建；`tests/test_user_system.py` | 已包含 |
| 邀请码 | `app/invites.py` operator-only CLI、`InviteCode` 约束、注册回滚和并发容量测试 | 已包含 |
| 登录 | `app/auth/service.py` 与 Flutter `AuthStore`/`LoginPage`；用户名规范化、错误收敛、Token 持久化 | 已包含 |
| JWT rotation | `auth_version` 写入 Token 并在 `get_current_user` 强校验；改用户名/密码后签发新 Token；测试覆盖 | 已包含 |
| 用户注销 | 后端按用户删除依赖记录并事务回滚；客户端标记待清理、分区 purge、失败可重试 | 已包含 |
| A/B 用户隔离 | 后端实体查询与写入均按 `user.id`；`test_tenant_isolation.py` 覆盖跨租户读写/恢复/统计 | 已包含 |
| local partition | `LocalRepository.forUser()`、Legacy 迁移、`FinanceRepository.ensureLocalOwner()`、`LocalStateSession.rebind()` | 已包含 |
| `0002_beta_users` | Alembic head 强制为 `0002_beta_users`；`test_migrations.py` 覆盖空库、旧库升级、冲突回滚 | 已包含 |
| tenant isolation | Account/Category/Transaction/Budget/Backup/Insights/Sync 服务均检查 `user_id` | 已包含 |
| offline/relogin | `lastVerifiedUserId`、离线恢复、登录切换、注销清理和故障恢复测试 | 已包含 |
| Sync session protection | `sessionGeneration`、`isCurrent`、`LocalStateSession` 串行写入与旧分区保护；Flutter/backend Sync 回归测试 | 已包含 |

## 验证记录

- Backend 用户系统、租户隔离、迁移和 Sync acceptance：`56 passed`。
- Flutter 全量测试：`261 passed`。
- Flutter 静态分析：无问题。
- 未发现需要从旧分支移植的功能缺口。

## Android APK 覆盖安装证据

证据工具为 Android SDK `apkanalyzer` 与 `apksigner`；没有读取 keystore 密码或私钥。

| 字段 | 当前 Beta APK | V1.0.0 候选 APK |
| --- | --- | --- |
| 文件 | `C:\Users\Admin\Downloads\随想记-Android-1.2.0-当前API.apk` | `E:\codex\suixiangji\outputs\wealthmate_flutter\build\app\outputs\apk\release\app-release.apk` |
| applicationId | `com.example.wealthmate_flutter` | `com.example.wealthmate_flutter` |
| versionName | `1.2.0` | `1.0.0` |
| versionCode | `2` | `3` |
| signing SHA-256 | `cadeb8ca7786b755305e07a086b407d8da7d6751d54566d9a0787d457df32458` | `cadeb8ca7786b755305e07a086b407d8da7d6751d54566d9a0787d457df32458` |

结论：applicationId 相同、证书指纹相同、`3 > 2`，`是否支持直接覆盖安装：YES`。当前 Android release 配置使用 `signingConfigs.getByName("debug")`，这解释了证书来源；在商店分发前必须继续使用同一已证明证书，不能换成未知签名。

发布配置状态：`PRODUCTION DOMAIN：NOT CONFIGURED`。正式 API 地址只能在最终域名明确后通过集中配置注入。

## 清理边界

删除前必须再次确认：

1. `main` 已包含发布提交，`v1.0.0` 与最终 `main` 指向同一 SHA。
2. 完整 Git bundle 已生成并通过 `git bundle verify`。
3. 工作区 clean、只有主工作目录。
4. 只删除开发分支；保留 `main`、`modular-monolith-v1`、`sync-v1.0-rc1`。
5. `feat/v1-admin` 在本次可见 refs 中未发现，状态为 `NOT VERIFIED`，不对其做删除或移植判断。
