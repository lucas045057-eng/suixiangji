# 随想记 V1.0.2 正式 Git 集成与发布前验收报告

日期：2026-09-16
正式分支：`release/v1.0.2`
范围：恢复真实 Git 历史、迁移本地 V1.0.2 修改、P0 回归、发布前验收与本地提交。

## Git 基线与安全边界

- origin/main before integration：`69c42c99c9d2d26844a58db3c2cb68f198f96478`
- 正式分支 baseline：`69c42c99c9d2d26844a58db3c2cb68f198f96478`
- Local snapshot baseline：`a8b385e2429b2aea3a21e8e2c6a6576c9f7fda47`
- Local snapshot final HEAD：`5aed524872a2e77d92500f1359bbfbf2e7719c82`
- Formal V1.0.2 final commit：以本次正式分支提交后的 `git rev-parse HEAD` 为准，最终交接中已单独记录具体 SHA。
- 迁移方式：先确认 `a8b385e` 与 `origin/main` 树无差异，再将 `git diff --binary a8b385e 5aed524` 应用到正式分支；未执行 unrelated histories merge、rebase、force push 或覆盖 main。
- 冲突：`0`；迁移后正式分支父提交直接来自真实 `origin/main`。
- 当前远端 tag：`modular-monolith-v1`、`sync-v1.0-rc1`、`v1.0.0`、`v1.0.1`；当前无 GitHub Release（[Release 页面](https://github.com/lucas045057-eng/suixiangji/releases)）。
- 本轮未 push、未创建新 tag、未创建 GitHub Release；未部署服务器、未修改生产数据库、未上传 APK。

## 版本与依赖

- Flutter package：`1.0.2+5`
- Flutter runtime：`kProductVersion = '1.0.2'`、`kProductBuild = 5`
- Backend latest：`1.0.2/5`
- Minimum supported：保持 `1.0.0/3`
- `cronet_http: 1.9.0` 与 `cronetHttpNoPlay=true` 保留。
- `python-jose` 已在 `outputs/wealthmate_backend/pyproject.toml` 的既有依赖声明中；本轮仅安装已有声明依赖，没有为本机环境问题修改依赖版本或新增依赖。

## P0 修复摘要

| P0 | Root cause | Final implementation | Regression evidence | Status |
|---|---|---|---|---|
| P0-1 中文金额解析 | 金额后缀与中文大数解析边界过宽，非法小数后缀可能落入整数/小数兜底 | 前后端统一金额语法、严格拒绝非法“厘/毫”组合，补齐“万”简写；所有自然语言输入继续先生成 draft，确认后才进入 Ledger | Flutter `finance_rules_test.dart`；后端 `test_quick_entry_module.py`/`test_domain.py` | PASS |
| P0-2 分类类型耦合 | 客户端候选过滤与服务端分类校验未形成同一约束边界 | 收入/支出切换自动清理非法 category，Ledger Store 与后端 CRUD/sync 复用类型校验，transfer 保持独立 | Flutter `transaction_form_category_test.dart`；后端 `test_category_type_coupling.py` | PASS |
| P0-3 双设备同步 | push/pull 游标、在途结果、共享队列和账号切换缺少完整会话边界 | 使用有状态同步协调器、watermark/cursor 分离、pending/冲突保护、owner partition 与会话代际校验；新身份可使旧恢复失效 | Flutter `two_device_sync_test.dart`、`v1_session_partition_test.dart`、`v1_lifecycle_edge_test.dart` 等 | PASS（fake A/B） |
| P0-4 余额校准 | 余额修正若建模为交易会污染现金流、预算和图表 | CNY 更新 `openingBalance`，外币仅更新 `openingCnyAmount`，只产生 account upsert 并保留汇率元数据 | `account_balance_calibration_test.dart` 及账户流程 UI 回归 | PASS |
| P0-5 Dashboard 查看全部 | 点击入口未稳定复用既有 Ledger tab 状态 | 移动端/桌面端共用既有 tab 选择逻辑，不创建第二个 Ledger route | `dashboard_navigation_test.dart` | PASS |

### 1. 中文金额解析与确认安全 — PASS

后端和 Flutter 统一支持 `16块86`、`16元8角6分`、`16元8毛6`、`两元`、`两块`、`两块五`、`2块5`、`16.86元`、`32元` 等表达；非法数字结构、零金额和无金额文本不会生成可直接入账的金额，确认门槛仍然保留。

涉及：`outputs/wealthmate_backend/app/quick_entry/`、`outputs/wealthmate_flutter/lib/domain/finance_rules.dart` 及对应测试。

证据：后端 `python -m pytest tests/test_quick_entry_module.py tests/test_domain.py -q` 为 `10 passed`；Flutter 全量回归包含 Quick Entry 用例并全部通过。

### 2. 收入/支出分类类型耦合 — PASS

分类候选按交易类型过滤，类型切换会清理不匹配选择；Ledger Store 与后端 CRUD/sync 共用边界校验，转账语义保持独立。

涉及：`outputs/wealthmate_flutter/lib/features/ledger/`、交易表单、`outputs/wealthmate_backend/app/ledger/service.py` 及分类耦合测试。

证据：`python -m pytest tests/test_category_type_coupling.py -q` 为 `4 passed, 1 warning`；相关 Flutter 分类/交易测试包含在正式分支 `318` 项全量回归中。

### 3. 双设备同步与会话隔离 — PASS（自动化 fake A/B 场景）

先用同账号、独立本地会话的 A/B fake server 场景复现问题，再按失败位置修复：

- push 返回的服务端 watermark 不再误作 pull 游标，避免 B 跳过 A 已接受的更早变更；
- push 失败时不继续 pull，保留 pending、游标和上次成功同步时间；
- 在途旧请求只能完成仍匹配的 `client_op_id`/实体快照，不能覆盖较新的本地编辑；
- 冲突恢复保护 transactions/accounts/categories/budgets 的较新 pending 操作；
- logout/relogin、重启、JWT gate 与 owner partition 恢复均保留游标和队列边界；
- 迟到的旧账号 push/pull 结果在新账号会话中被丢弃；
- 未物化的新用户分区与已持久化空状态保持 API 兼容，同时首次变更可以正确使用功能初始快照。

未增加同步 payload 字段，未修改 JWT 公共协议，未修改数据库 schema/migration。

涉及：`outputs/wealthmate_flutter/lib/core/sync/sync_coordinator.dart`、`lib/core/database/local_state_session.dart`、`lib/data/finance_repository.dart`、`lib/data/local_repository.dart`、`lib/state/finance_store.dart` 及同步测试。

证据：正式分支同步/P0 专项命令共 `88` 项通过；其中 fake A/B 双设备、同步 coordinator、auth gate、session partition 与生命周期场景均通过，并包含在全量 `318` 项中。

### 4. 余额校准 — PASS

余额调整改为账户级 calibration：CNY 修改 `openingBalance`；外币只修改 `openingCnyAmount`，保留原币 opening、汇率、日期和来源；交易、月度指标、预算与图表不被伪造交易改变，队列只产生 account upsert。

涉及：`outputs/wealthmate_flutter/lib/features/assets/`、`lib/ui/account_detail_page.dart`、财富/设置文案及校准测试。

证据：余额校准、资产 Store、账户流程聚焦回归通过，包含校准后再次保存配置的 UI 回归场景。

### 5. Dashboard“查看全部” — PASS

移动端和桌面端共用既有页面选择逻辑，打开已有 Ledger tab，不新增账目 route。

证据：`dashboard_navigation_test.dart`、`widget_test.dart`、`overview_pages_test.dart` 聚焦回归 `7` 项通过。

## 测试证据

| 检查 | 结果 | 证据 |
|---|---|---|
| Node/Web | PASS | `npm test`：`7 passed, 0 failed` |
| Flutter 静态分析 | PASS | `flutter analyze`：`No issues found!` |
| Flutter 全量测试 | PASS | `flutter test --no-pub --reporter compact`：`318` 项通过 |
| Flutter 版本与更新 | PASS | `v1.0.2_version_test.dart` + `app_version_test.dart`：`8` 项通过 |
| 后端金额/域回归 | PASS | `python -m pytest tests/test_quick_entry_module.py tests/test_domain.py -q`：`10 passed` |
| 后端分类耦合 | PASS | `4 passed, 1 warning`（单文件运行） |
| 后端版本/健康检查 | PASS | `4 passed` |
| 后端全套测试 | PASS | 正式分支 `python -m pytest tests -q --basetemp E:\suixiangji\work\pytest-formal-full`：`138 passed, 2 warnings` |
| Android release APK | NOT VERIFIED | 正式分支 `flutter build apk --release --verbose --dart-define=cronetHttpNoPlay=true`；Gradle `:app:compileFlutterBuildRelease` 失败，未生成 APK。 |
| Windows symlink | NOT APPLICABLE | Flutter 测试可正常运行，未遇到 Developer Mode/symlink 阻塞，因此没有使用 BLOCKED 标记。 |

后端分类测试的临时目录/全局 engine 顺序隔离问题已通过测试夹具隔离修复；未修改生产依赖或业务代码来绕过本机环境问题。

### Android release 构建诊断

verbose 日志保存在 `E:\suixiangji\work\android-build-formal.log`。本次构建耗时约 `180.6s`，不是无期限 hang：Gradle/JDK/NDK 均已启动并继续执行 AOT，失败点是 `sqlite3` native asset hook 下载 `libsqlite3.arm.android.so` 时访问 GitHub Release 资源发生 HTTP 超时，随后 `compileFlutterBuildRelease` 以 exit code `1` 结束。

- JDK：`21.0.6`
- Gradle：`9.3.1`
- Android Gradle Plugin：`9.1.0`
- NDK：`28.2.13676358`
- `cronet_http 1.9.0` 已从本地 pub cache 发现，未观察到业务依赖解析错误。
- 结论：这是构建阶段的外部网络下载失败，不是 Flutter symlink 阻塞，也没有通过删除 sqlite3、绕过 native asset 或修改业务架构来出包。

### 设备验收

- Android smoke test：`NOT RUN`，因为没有生成 APK。
- `REAL TWO-DEVICE TEST: NOT RUN`；已通过独立 fake A/B 会话自动化，不能替代真实设备验收。
- Flutter 测试没有遇到 Windows Developer Mode/symlink 阻塞，因此不使用 `BLOCKED: Windows Developer Mode / symlink requirement` 标记。

## 数据兼容性与发布边界

- 对比正式 `origin/main` 的迁移文件列表未发现 migration/schema/Drift database 文件变更。
- API 公共协议未修改；sync payload 未增加字段；push/pull 的修复只调整客户端状态边界；minimum supported 仍为 `1.0.0/3`。
- 现有 V1.0.1 用户数据不需要迁移；本地 owner partition 处理保留已有数据边界，未执行生产数据操作。
- 未执行生产环境操作、服务器部署、生产数据库写入、远端 push、tag、Release 或 APK 测试用户分发。
- 最终工作区应由交接命令 `git status --short --branch` 确认干净；最终本地 HEAD 以报告提交后的 `git rev-parse HEAD` 为准。

## Release readiness

当前不是 `READY FOR RELEASE REVIEW`。仍有以下 blocker：

1. Android release APK 未生成，阻塞原因是 sqlite3 native asset 的外部 GitHub 资源下载超时。
2. Android smoke test 尚未执行。
3. `REAL TWO-DEVICE TEST: NOT RUN`。

本轮正式分支提交仅用于本地验收，等待用户确认后再决定 push / merge / tag / Release / deployment。
