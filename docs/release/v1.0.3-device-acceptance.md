# V1.0.3 Android Device Acceptance Gate

**Recorded:** 2026-09-16
**Artifact:** V1.0.3 build 6, package `com.example.wealthmate_flutter`, signed certificate `cadeb8ca7786b755305e07a086b407d8da7d6751d54566d9a0787d457df32458`.

## Device and overlay result

The Android SDK reported one authorized real device: serial `10AEB13E0200130`, model `V2426A`, Android `16`.

Before installation, the device contained package `com.example.wealthmate_flutter` at version `1.0.1 / 4`, with data directory `/data/user/0/com.example.wealthmate_flutter` and first-install time `2026-09-16 15:10:34`.

The V1.0.3 artifact was installed with `adb install -r` and returned `Success`. No uninstall, downgrade, or data-clear flag was used.

After installation, the package reported version `1.0.3 / 6`, the same data directory, and the unchanged first-install time; only the update time and APK code path changed. This is the required single-device overlay-install and data-preservation evidence.

## Single-device smoke result

- The app launched directly into the existing signed-in account rather than the login page or Demo Mode. The home screen showed the existing greeting, financial snapshot, ledger data, and `同步服务已配置`.
- The synchronization button completed and the home status updated to `已同步 · 20:17`, demonstrating the production API path from the installed APK.
- Home navigation exposed `快捷记`, `查看全部`, `同步`, and the budget section. The ledger page showed existing entries, and the add-entry form opened and was cancelled without creating data.
- The statistics page opened and showed current expense summaries. The wealth page showed existing accounts; an account configuration page exposed default-payment and balance-calibration controls.
- The account/login page opened while retaining the signed-in identity; the default-payment account selector and budget page opened successfully. The budget form opened and was cancelled without changing stored data.
- Existing account names, ledger entries, categories, budget navigation, login identity, and sync state remained visible after the upgrade. No application data was edited or deleted during smoke testing.

## Deferred gate

REAL TWO-DEVICE TEST: NOT RUN

Two-device A/B synchronization is deferred at the user request. No second device was required or used, and no attempt was made to represent this deferred check as passed.

OVERLAY INSTALL: PASS

SINGLE-DEVICE DATA, LOGIN, AND SMOKE: PASS

DEVICE ACCEPTANCE: DEFERRED PENDING TWO-DEVICE SYNC

The single-device checks are complete. The two-device synchronization gate remains explicitly deferred; APK release completion is therefore not claimed. Backend service deployment may proceed only as the separately authorized service step, while APK upload, update-metadata switch, tag, and GitHub Release remain withheld.
