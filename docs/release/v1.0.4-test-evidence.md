# V1.0.4 Test Evidence

**Recorded:** 2026-09-20
**Branch:** `fix/v1.0.4-sync-and-inapp-update`  
**Scope:** serialized sync drain, PostgreSQL version allocation, Android in-app update flow, and release metadata.

## Automated results

| Area | Command/result | Status |
| --- | --- | --- |
| Flutter analysis | `flutter analyze` — No issues found | PASS |
| Client sync/lifecycle regression | Targeted Flutter suite — 67 tests passed | PASS |
| App update/download/install regression | Targeted Flutter suite — 27 tests passed | PASS |
| Flutter version regression | `flutter test test/v1.0.4_version_test.dart` — 2 passed | PASS |
| Android build-guard regression | `tools/build-android-release.tests.ps1` — 18 passed | PASS |
| Full Flutter suite | `flutter test` — 367 passed | PASS |
| Backend metadata regression | `pytest -q tests/test_app_version.py tests/test_v1_0_4_release_metadata.py` — 3 passed | PASS |
| Backend full suite | `pytest -q` — 139 passed, 3 skipped, 1 warning, 17 subtests passed | PASS; PostgreSQL rows are separately marked below |
| PostgreSQL concurrency regression | 3 real-PostgreSQL tests skipped because `WEALTHMATE_POSTGRES_TEST_DATABASE_URL` was unavailable | **NOT VERIFIED** |
| Android V1.0.4 release APK | Rebuilt after runtime-source change; guarded production build, independent inspector, formal certificate continuity, and Embedded Cronet library check | PASS |
| Real Android device acceptance | No overlay install or device smoke test was run | **NOT VERIFIED** |

## PostgreSQL gate

The committed regression harness covers concurrent pushes, strict per-user versions, locked `User` freshness after identity-map staleness, replay idempotency, and rollback. The required real-PostgreSQL execution could not be performed on this host:

- no `WEALTHMATE_POSTGRES_TEST_DATABASE_URL` was configured;
- Docker CLI was installed, but the Linux daemon was unavailable at `dockerDesktopLinuxEngine`;
- no production database or live-user rows were accessed.

Therefore the required “old implementation fails on real PostgreSQL, repaired implementation passes” evidence remains **NOT VERIFIED**.

The backend full-suite rerun now uses both the workspace-local pytest temporary directory and an isolated `test_api` SQLite engine. Its ordinary tests pass. The three real-PostgreSQL concurrency cases remain skipped because `WEALTHMATE_POSTGRES_TEST_DATABASE_URL` is unavailable; this remains **NOT VERIFIED** for the real PostgreSQL gate only.

## Android build diagnosis

The first clean build reproduced two independent failures:

- Kotlin incremental cache registration failed while closing caches for several Flutter plugins. The project-level `kotlin.incremental=false` setting removes that host-toolchain failure without changing application or synchronization behavior.
- The default `cronetHttpNoPlay=false` dependency path pulled Play Services Cronet `141.7340.3`, which AGP 9.1 rejected because `org.chromium.net` was declared by both `cronet-api` and `cronet-shared`. The guarded release path already requires `cronetHttpNoPlay=true`; the successful APK contains `libcronet.143.0.7445.0.so` for all three packaged ABIs.

The release guard initially failed to inspect the APK because the host did not expose `JAVA_HOME` to `apkanalyzer.bat`. The inspector now resolves the installed Android Studio JBR locally and the same guarded build passes without a manually exported Java environment.

## Full regression failure resolution

The initial full-suite reproduction recorded five Flutter failures and six backend test failures. The bare backend command also produced 64 Windows pytest temporary-directory setup errors; those were test-infrastructure failures and are recorded below rather than counted as business failures.

| Test | Classification | Root cause | Fix | Final result |
| --- | --- | --- | --- | --- |
| `test/architecture/module_boundaries_test.dart`: feature/UI layers do not reach transport internals | B — real boundary regression | `AppUpdateStore` imported `ApiFailure` from the transport layer only to format an error | Removed the transport type dependency and retained the same user-facing `toString()` message contract | PASS |
| `test/architecture/module_boundaries_test.dart`: production HTTP construction stays inside the platform factory | A — old test/design mismatch | Approved V1.0.4 design gives the streamed HTTPS APK downloader its own Dart `HttpClient` boundary; the global API-factory scan treated it as an API client | Scoped the architecture assertion to shared API transport and documented the approved App Update downloader exception | PASS |
| `dc03_synced_edit_semantics_red_test.dart`: RED-SYNC-03 retry preserves pending edit operation ID | A — old test timing assumption | The test inspected the queue after `pumpAndSettle()` while the new debounced automatic drain had already attempted the push | Injected two controlled transient failures so the test observes durable retry state, then verifies every retry keeps the same operation ID | PASS |
| `dc03_synced_edit_semantics_red_test.dart`: RED-SYNC-04 committed edit creates a new operation | A — old test timing assumption | The second edit's automatic drain could complete before the assertion inspected pending state | Injected one controlled failure for the second automatic round, preserving the queue for the operation-identity assertion | PASS |
| `quick_entry_store_test.dart`: online confirmation syncs once after Ledger mutation | A — old test completion assumption | Confirmation persists the mutation and schedules a debounced sync; confirmation itself does not promise the network round is complete | The test explicitly awaits `requestSync(..., immediate: true)` before asserting push/pull counts | PASS |
| `test_api.py::test_agent_endpoint_only_returns_a_draft` | C — test/fixture infrastructure | `test_api` changed the SQLite URL after `app.db` had already created an engine against a prior database, exposing old rows | Rebound the existing engine/sessionmaker after test settings are installed while preserving model metadata | PASS |
| `test_api.py::test_health_and_idempotent_transaction_create` | C — test/fixture infrastructure | Same cross-suite database contamination | Same isolated engine/sessionmaker rebinding | PASS |
| `test_api.py::test_sync_push_handles_account_dependency_when_transaction_precedes_account` | C — test/fixture infrastructure | Same stale database fixture made dependency-order assertions observe pre-existing entities | Same isolated engine/sessionmaker rebinding | PASS |
| `test_api.py::test_sync_push_handles_category_dependency_when_budget_precedes_category` | C — test/fixture infrastructure | Same stale database fixture | Same isolated engine/sessionmaker rebinding | PASS |
| `test_api.py::test_sync_push_handles_category_dependency_when_transaction_precedes_category` | C — test/fixture infrastructure | Same stale database fixture | Same isolated engine/sessionmaker rebinding | PASS |
| `test_api.py::test_transfer_is_excluded_from_stats_and_backup_round_trips` | C — test/fixture infrastructure | Same stale database fixture changed the aggregate totals | Same isolated engine/sessionmaker rebinding | PASS |

The 64 setup errors came from the host's ACL-protected shared `C:\Users\Admin\AppData\Local\Temp\pytest-of-Admin` root. `tests/conftest.py` now points pytest's generated temporary files to the ignored workspace-local `.pytest-tmp` directory; the exact `pytest -q` command then completed with 139 passed and 3 expected PostgreSQL skips.

The build guard also received a validation-only correction after the rebuild exposed a Windows console-width issue: long certificate digests can wrap in `Format-List` output, so the parser now joins exactly 64 hexadecimal characters before comparison. `tools/build-android-release.tests.ps1` completed with 18 passed.

## RED-before-implementation record

- PostgreSQL race/rollback tests: `c8bd858` (test-only commit).
- Sync drain/lifecycle RED contracts: `1fe8a2f`, `458552f`, `419db6e`.
- In-app update RED contracts: `38d2263` — 7 initial passes and 19 intentional failures before production implementation.

## Safety boundary

No production deployment, production database modification, production `/app/version` switch, GitHub main/tag/release operation, force push, app uninstall/data clear, device installation, or Android signing-identity change was performed.
