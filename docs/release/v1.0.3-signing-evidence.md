# V1.0.3 Android Signing Evidence Gate

**Recorded:** 2026-09-16
**Scope:** Task 2 only; no APK was built, signed, installed, uploaded, or released.

## Repository and reference checks

- Active worktree branch: `hotfix/v1.0.3-android-release` at `c283a001bf57ac8b1b5cd33e5476e4577edf8e7d`.
- `HEAD`, `main`, and `origin/main` each resolve to `c283a001bf57ac8b1b5cd33e5476e4577edf8e7d`.
- `v1.0.2` remains an annotated tag whose tag-object SHA is `cd6e86bafb8dd5aa8fa62f5cd782d353779b0f46`.
- The worktree already contained the approved V1.0.3 design and plan as untracked files. They were not modified or staged.

## Inspection method

`tools/inspect-android-signing.ps1` accepts an APK path and an Android SDK path. For an available APK it reports only package name, version name, version code, APK SHA-256, and signing-certificate SHA-256. It accepts no keystore, password, alias, or secret input and returns exit code `2` when the APK or required Android SDK tool is unavailable.

The repository's Android ignore rules already ignore `key.properties`, `*.jks`, and `*.keystore`; no `.gitignore` change was needed.

## Actual evidence collected

- Recursive worktree inspection found no APK, AAB, JKS, keystore, or `key.properties` file.
- Git tracked-file and complete Git path-history inspection found no APK, AAB, JKS, keystore, or `key.properties` path.
- `adb devices` listed no connected Android device, so no installed V1.0.1 package could be inspected.
- Android SDK tooling (`apkanalyzer`, `apksigner`, and `adb`) is available; no V1.0.1 or V1.0.2 APK was available to supply to the inspection tool.
- No original signing private key, alias, or controlled access method is available within the allowed worktree scope. No secret files were opened or printed.

## Signing comparison

| Artifact | APK SHA-256 | Package/version | Signing certificate SHA-256 | Result |
| --- | --- | --- | --- | --- |
| V1.0.1 baseline | Not available | Not available | Not available | Cannot establish baseline identity |
| V1.0.2 artifact | Not available | Not available | Not available | Cannot compare identities |
| V1.0.3 candidate | Not built | Not built | Not available | Not attempted |

No root cause is asserted: the unavailable evidence does not establish whether V1.0.2 used a different signing identity.

## Gate result

RELEASE BLOCKED: ORIGINAL SIGNING PRIVATE KEY NOT AVAILABLE

RELEASE STATUS: BLOCKED

Release work must not continue until the original private signing key and a V1.0.1 APK or equivalent verifiable V1.0.1 certificate evidence are supplied through an approved controlled source. The required next comparison is an identical signing-certificate SHA-256 for the V1.0.1 baseline and the future V1.0.3 candidate.
